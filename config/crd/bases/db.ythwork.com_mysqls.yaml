---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: mysqls.db.ythwork.com
spec:
  group: db.ythwork.com
  names:
    kind: MySQL
    listKind: MySQLList
    plural: mysqls
    singular: mysql
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MySQL is the Schema for the mysqls API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MySQLSpec defines the desired state of MySQL
            properties:
              appPasswordSecretName:
                description: Secret name containing app user password
                type: string
              appUser:
                default: appuser
                description: Application user name
                type: string
              database:
                default: appdb
                description: Application database name
                type: string
              image:
                default: mysql:8.0
                description: MySQL container image
                type: string
              port:
                default: 3306
                description: MySQL port
                format: int32
                minimum: 1
                type: integer
              replPasswordSecretName:
                description: Secret name containing replication password
                type: string
              replicas:
                default: 3
                description: |-
                  Total number of MySQL pods
                  primary 1 + replicas N
                format: int32
                minimum: 1
                type: integer
              rootPasswordSecretName:
                description: Secret name containing root password
                type: string
              storageSize:
                default: 10Gi
                description: Storage size for each pod
                type: string
            type: object
          status:
            description: MySQLStatus defines the observed state of MySQL
            properties:
              message:
                type: string
              phase:
                type: string
              primaryPod:
                type: string
              primaryService:
                type: string
              readyReplicas:
                format: int32
                type: integer
              replicaService:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
